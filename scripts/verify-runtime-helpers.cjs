async function getPage(endpoint){const targets=await fetch(endpoint).then(r=>r.json());const page=targets.find(t=>t.type==='page');if(!page)throw new Error('renderer target not found');return page;}
async function authenticate(page){return evaluate(page.webSocketDebuggerUrl,`(async()=>{const s=await window.deskforge.auth.status();if(s.authenticated)return false;if(s.needsSetup)await window.deskforge.auth.register({username:'reactowner',displayName:'React Owner',password:'Deskforge123'});else await window.deskforge.auth.login({username:'reactowner',password:'Deskforge123'});location.reload();return true})()`);}
const wait=ms=>new Promise(r=>setTimeout(r,ms));
function evaluate(url,expression){return new Promise((resolve,reject)=>{const socket=new WebSocket(url);const timer=setTimeout(()=>{socket.close();reject(new Error('CDP evaluation timed out after 30 seconds'));},30000);socket.addEventListener('open',()=>socket.send(JSON.stringify({id:1,method:'Runtime.evaluate',params:{expression,returnByValue:true,awaitPromise:true}})));socket.addEventListener('message',event=>{const message=JSON.parse(event.data);if(message.id!==1)return;clearTimeout(timer);socket.close();if(message.result.exceptionDetails)reject(new Error(message.result.exceptionDetails.exception?.description||message.result.exceptionDetails.text));else resolve(message.result.result.value)});socket.addEventListener('error',()=>{clearTimeout(timer);reject(new Error('CDP connection failed'));});});}
module.exports={getPage,authenticate,evaluate,wait};
